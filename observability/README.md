# Observability

Start after the application network exists:

```bash
docker compose -f observability/compose.yaml up -d
```

Codespaces ports: Grafana 3000, Prometheus 9090, cAdvisor 8088.
Demo Grafana: `admin / logistpulse_demo`.
